<%= form_for(@comment) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field">
  	<%= f.hidden_field :post_id, value: post.id %>
    <%= f.text_area :body, placeholder: "Compose a new comment" %>
  </div>
  <%= f.submit "Post", class: "btn btn-primary" %>
<% end %>