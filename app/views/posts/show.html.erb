<% provide(:title, "#{@user.first_name} #{@user.last_name} | FakeBook") %>
<div class="row">
  <section class="user_info">
    <h1>
      <%= "#{@user.first_name} #{@user.last_name}" %>
    </h1>
    <p>
    	<%= @post.content %>
    </p>
  </section>
</div>